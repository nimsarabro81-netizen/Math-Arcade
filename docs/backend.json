
{
  "entities": {
    "UserRank": {
      "title": "UserRank",
      "type": "object",
      "description": "Stores user ranking information for the VectorZen app.",
      "properties": {
        "userId": {
          "type": "string",
          "description": "Reference to the User."
        },
        "username": {
            "type": "string",
            "description": "The user's display name, 'Anonymous' for anonymous users."
        },
        "score": {
          "type": "number",
          "description": "The user's score in the VectorZen game."
        },
        "lastUpdated": {
          "type": "string",
          "description": "Timestamp of the last time the user's score/rank was updated.",
          "format": "date-time"
        }
      },
      "required": [
        "userId",
        "username",
        "score",
        "lastUpdated"
      ]
    },
    "AlgebraRank": {
      "title": "AlgebraRank",
      "type": "object",
      "description": "Stores user ranking information for the Algebra Arena game.",
      "properties": {
        "userId": {
          "type": "string",
          "description": "Reference to the User."
        },
        "username": {
          "type": "string",
          "description": "The user's display name, 'Anonymous' for anonymous users."
        },
        "score": {
          "type": "number",
          "description": "The user's score in the Algebra Arena game."
        },
        "lastUpdated": {
          "type": "string",
          "description": "Timestamp of the last time the user's score/rank was updated.",
          "format": "date-time"
        }
      },
      "required": [
        "userId",
        "username",
        "score",
        "lastUpdated"
      ]
    },
    "EquationRank": {
        "title": "EquationRank",
        "type": "object",
        "description": "Stores user ranking information for the Equation Equilibrium game.",
        "properties": {
            "userId": { "type": "string", "description": "Reference to the User." },
            "username": { "type": "string", "description": "The user's display name, 'Anonymous' for anonymous users." },
            "score": { "type": "number", "description": "The user's score in the Equation Equilibrium game." },
            "lastUpdated": { "type": "string", "description": "Timestamp of the last time the user's score/rank was updated.", "format": "date-time" }
        },
        "required": ["userId", "username", "score", "lastUpdated"]
    },
    "GameSetting": {
      "title": "GameSetting",
      "type": "object",
      "description": "Stores the active status of a game.",
      "properties": {
        "id": {
          "type": "string",
          "description": "The unique identifier for the game (e.g., 'vector-zen')."
        },
        "isActive": {
          "type": "boolean",
          "description": "Whether the game is currently active and visible to players."
        }
      },
      "required": ["id", "isActive"]
    },
    "User": {
      "title": "User",
      "type": "object",
      "description": "Represents a user in the VectorZen app.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the user."
        }
      },
      "required": [
        "id"
      ]
    }
  },
  "auth": {
    "providers": [
      "anonymous",
      "email"
    ]
  },
  "firestore": {
      "collections": [
        {
            "path": "/userRanks",
            "schema": {
                "$ref": "#/entities/UserRank"
            },
            "description": "Stores rankings for all users for the VectorZen game."
        },
        {
            "path": "/algebraRanks",
            "schema": {
                "$ref": "#/entities/AlgebraRank"
            },
            "description": "Stores rankings for all users for the Algebra Arena game."
        },
        {
            "path": "/equationRanks",
            "schema": {
                "$ref": "#/entities/EquationRank"
            },
            "description": "Stores rankings for all users for the Equation Equilibrium game."
        },
        {
          "path": "/gameSettings",
          "schema": {
            "$ref": "#/entities/GameSetting"
          },
          "description": "Stores the active status for each game."
        }
      ]
  }
}
